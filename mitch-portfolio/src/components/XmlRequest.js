import React, { useState, useEffect } from 'react';

const XmlCall = () => {

    // Make API call
    useEffect(() => {
        const xhr = new XMLHttpRequest();
        xhr.open();
        // setItems(items);
        // fetchItems();
    }, []);
}

export default XmlCall;